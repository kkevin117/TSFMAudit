# Checkpoints Directory

Model checkpoints and downloaded weights are not included in this anonymous repository.

Use `CHECKPOINT_ROOT` to point scripts to local checkpoints:

```bash
export CHECKPOINT_ROOT=/path/to/checkpoints
```

For VisionTS, set `VISIONTS_CKPT_DIR` if the checkpoint directory differs from `$CHECKPOINT_ROOT/visionts`.
